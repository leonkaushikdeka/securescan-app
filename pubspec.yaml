name: securescan
description: Local Deepfake and Phishing Detection App with Background Scanning

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  tflite_flutter: ^0.12.1
  image_picker: ^1.1.2
  image: ^4.2.0
  permission_handler: ^11.3.0
  sqflite: ^2.3.3
  path: ^1.9.0
  path_provider: ^2.1.3
  url_launcher: ^6.3.0
  shared_preferences: ^2.2.3
  workmanager: ^0.5.2
  flutter_local_notifications: ^17.2.2

  cupertino_icons: ^1.0.2
  google_fonts: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/models/
    - assets/data/
